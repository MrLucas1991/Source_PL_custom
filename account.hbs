{{!< default}}

<main class="gh-main">
    <article class="gh-article gh-canvas gh-account-container">
        <header class="gh-article-header gh-account-header">
            <h1 class="gh-article-title is-title">Moje konto</h1>
            <p class="gh-article-excerpt is-body">Zarządzaj swoimi danymi i subskrypcjami.</p>
        </header>

        <section class="gh-content gh-canvas is-body">
            {{#if @member}}
                <form class="gh-form gh-account-form" data-members-form>
                    <label for="account-email" class="gh-account-label">E-mail</label>
                    <input class="gh-account-input" id="account-email" name="email" type="email" value="{{@member.email}}" readonly>

                    <p class="gh-account-status {{#if @member.paid}}active{{else}}inactive{{/if}}">
                        <strong>Status subskrypcji:</strong> 
                        {{#if @member.paid}}
                            Aktywna
                        {{else}}
                            Brak subskrypcji
                        {{/if}}
                    </p>

                    <a class="gh-account-button primary" href="/account-billing/">Zarządzaj subskrypcją</a>

                    <button class="gh-account-button secondary" type="submit" data-members-signout aria-label="Wyloguj się">
                        <span>Wyloguj się</span>
                        {{> "icons/loader"}}
                    </button>
                </form>

                <p class="gh-account-helper">
                    Masz problem? <a href="{{@site.url}}/kontakt/">Skontaktuj się z nami</a>.
                </p>
            {{else}}
                <p class="gh-account-helper">
                    Nie jesteś zalogowany. <a href="{{@site.url}}/login/">Zaloguj się</a> lub <a href="{{@site.url}}/signup/">Zarejestruj się</a>.
                </p>
            {{/if}}
        </section>
    </article>
</main>
